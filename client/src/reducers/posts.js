// Reducers: function
// redux state == posts
export default (posts = [], action) => {

  switch (key) {
    case 'FETCH_ALL':
      return posts

    case 'CREATE':
      return posts
  
    default:
      break;
  }
}
